# Rename findings

- The repo was already public before this rename.
- External repo slug is now `qiao-wechat`.
- Internal package rename touches imports, packaging metadata, CLI entry point, tests, and default SQLite path.
- README still had stale Windows-local path references after the first rename pass.
- Theme metadata still referenced `wx-content-mesh` as source.
