# qiao-wechat rename plan

## Goal

Unify the public and internal technical name from `wx-content-mesh` / `wx_content_mesh` to `qiao-wechat` / `qiao_wechat` without breaking the current publish workflow.

## Phases

- [x] Inventory naming touchpoints
- [x] Rename package/module, CLI name, and imports
- [ ] Clean metadata/docs/runtime defaults
- [ ] Run focused verification and fix fallout

## Notes

- Keep repo root directory name unchanged for now.
- Keep behavior-focused code changes out of scope.
